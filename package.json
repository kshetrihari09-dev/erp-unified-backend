{
  "name": "erp-unified-backend",
  "version": "2.2.0",
  "description": "MediERP — Unified Node.js + PostgreSQL backend: Pharma ERP + Bank-Grade Accounting Engine",
  "main": "src/server.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development node src/server.js",
    "start:prod": "cross-env NODE_ENV=production node src/server.js",
    "start:staging": "cross-env NODE_ENV=staging node src/server.js",
    "dev": "cross-env NODE_ENV=development nodemon src/server.js",
    "migrate": "knex migrate:latest",
    "migrate:rollback": "cross-env NODE_ENV=development knex migrate:rollback",
    "migrate:prod": "cross-env NODE_ENV=production knex migrate:latest",
    "migrate:prod:rollback": "cross-env NODE_ENV=production knex migrate:rollback",
    "seed": "cross-env NODE_ENV=development knex seed:run",
    "seed:prod": "cross-env NODE_ENV=production knex seed:run",
    "setup": "npm run migrate && npm run seed",
    "setup:prod": "npm run migrate:prod && npm run seed:prod",
    "gen-certs": "bash scripts/gen-certs.sh",
    "check-env": "node -e \"require('./src/config/index.js'); console.log('✅ Config OK')\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.5.1",
    "express-validator": "^7.2.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "knex": "^3.1.0",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^9.0.1",
    "pg": "^8.12.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.4"
  }
}